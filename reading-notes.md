# 《Designing Multi-Agent Systems》

# ch1 introduction
1. 多智能体视角可以通过"选择性上下文供给"来弥补这一局限——也就是说，只把与某项任务相关的上下文片段（例如迄今为止已执行的操作历史）提供给专注于该任务的智能体。
   只要以减少不必要认知负荷的方式来组织指令、管理上下文，我们就能显著提升 AI 系统的任务表现

"自我编排"：干活的顺序、人员的组合、上下的衔接，在运行中动态决定，而不是提前写死。API 报错了，
   带班的可以现场决定"换个数据源试试"或"上报人类"，而不是整个流程崩盘


agent 不是函数，是"装着决策函数、有眼有手、在会变的世界里持续转圈干活"的系统；环境不是输入输出的接口板，是"给它出题、接它动作、被它改变后再出新题"的活世界。
与纯 LLM 调用的本质区别：

纯调用 = 函数：问一次 → 答一次 → 完，无状态、无循环、环境不变
Agent = 进程：干活 → 看结果 → 不满意再干 → 直到搞定，有状态、有循环、环境随动作演化
工程上的直接推论：Agent 是有状态的循环进程，所以你必须设计循环退出条件、超时、状态持久化、中间步骤可观测性——这些在一次性推理调用里都不存在。

> 训练层：现在主流模型都经过专门的后训练——SFT 喂大量"思考→发出工具调用→读返回→继续"的轨迹数据，
         RL 再按"任务最终完成没"给奖励.这一步让模型会在合适时机生成合法的工具调用格式。
> 还有架构层：就算模型训练得再好，"无缝融合"另一半靠运行时脚手架.工具的描述（名字、参数、用法）是塞进上下文的，
               模型现场读、现场决定用哪个——新加一个工具不用重新训练，改 prompt 就行 模型输出的"我要调 API"只是一段文本，
               是外面的框架解析它、真去执行、把结果作为新消息喂回去

2. Why now?
   人工 Agent 如何与自身及人类协作（机器人规划、导航、人机协作、群体智能）都被广泛研究过。
   但 Agent 的发展长期受限：缺一个能适应上下文、综合出计划、并驱动计划所需行动的推理引擎（人工大脑）。
   GPT-3.5 和 GPT-4 等模型展示的推理能力改变了现状，为真正自主的多 Agent 系统提供了关键组件

   多 Agent 系统能自动从底层 AI 模型的进步中受益

   很多重复性问题今天难以用可靠软件自动化。研究表明部分困难在于这些任务需要隐性知识（tacit knowledge）——难以言明或编撰成规则、
   更容易通过经验和实践传递的知识。LLM 驱动的 Agent 群编码了海量知识、能与人和其他 Agent 协作，
   为解决这类复杂任务提供了新机会——把以前认为不可能自动化的任务自动化，减少人力和错误


3. 架构选择遵循一个基于任务特征的逻辑递进：

从最根本的问题开始：你的任务需要超越文本生成的行动吗？如果任务只涉及文本处理、分析或生成——比如基于训练数据回答问题、文档摘要、按规格生成代码——那么直接调用 AI 模型生成响应就够了。

对于需要行动的任务，下一个考量是：你的解决路径是否已知、能否表达为工作流？当你清楚需要哪些步骤、该怎么执行时，你面对的选择是单 Agent 还是多 Agent 工作流。

如果你的工作流受益于专门化分工——任务的不同部分需要不同的领域知识——那么多 Agent 工作流是合适的。这种方法在专门化 Agent 间的协作模式已被充分理解、每个 Agent 的角色和交接遵循既定流程时效果最好。例如金融分析流水线：行情数据、风险评估、组合优化各由专门 Agent 负责，按定义好的顺序工作。但如果一种专长就能有效处理整个任务，一个配齐必要工具的单 Agent 效率更高。

对于解决路径不明确的任务，你在单 Agent 和自主多 Agent 系统之间选择，依据是所需的探索程度。如果任务涉及已知动作序列、只需相对简单的行动—感知循环（如带结果校验的 API 调用），单 Agent 合适。
但以下情况自主多 Agent 系统变得必要：解法需要通过探索和动态 Agent 交互来涌现；Agent 需要被独立定义和改进；Agent 必须根据中间结果调整协作方式；或通过从成败中迭代学习来提升性能。例如应用开发——需求和实现方式在实验中逐步演化。

一条指导原则：选择能有效满足需求的最简单架构。 每一层复杂度的提升都带来额外的开发时间和潜在故障点，只换来更大的能力与适应性。拿不准时，从简单的开始，随着对任务真实需求的理解加深再演化架构





























